# v7.4.0 Agent Tools Validation Matrix

Release gate for Agent-scoped tool access, immutable runtime generations, Context removal,
and the three-Agent first-run experience. Automated results below are from the feature
branch; supervised development-server and live-call rows must pass before tagging.

## Candidate

- **Branch:** `v7.4.0-agent-tools`
- **Base:** `origin/main` at `d8916cf7` (`v7.3.5`)
- **Development server:** `10.44.0.104`
- **Live validation target after development sign-off:** `voiprnd`

## Automated and static gates

| Gate | Expected | Status |
|---|---|---|
| Full Python suite | No regressions | PASS — 1,572 passed, 7 skipped |
| Agent policy/runtime/reload focused tests | inherit/selected/none, stale/empty fail-closed, generation isolation, reload rollback/409 | PASS |
| Legacy import tests | atomic complete import, collisions, idempotency, invalid rollback, populated-store preservation | PASS |
| Starter Agent tests | exactly Receptionist/Sales/Support, Receptionist default, idempotent | PASS |
| Frontend Vitest | Agent policy round-trip plus existing UI tests | PASS — 163 passed |
| Frontend lint | No warning-regression beyond pinned baseline | PASS — 867 warnings, 0 errors (limit 868) |
| Frontend production build | TypeScript/Vite build completes | PASS |
| Diff hygiene | no whitespace errors; unrelated user files untouched | PASS |

## Development-server gates

| Area | Procedure | Status |
|---|---|---|
| Upgrade migration | Start with legacy Contexts and empty Agent store; verify complete import and default | PENDING |
| Existing Agent safety | Start with populated `agents.db`; verify zero overwrite/reseed | PENDING |
| Removed Context UI | Confirm no sidebar/palette entry; `/contexts` notice appears once then redirects | PENDING |
| Empty first run | Complete wizard; verify exactly three Agents and no transfer routes | PENDING |
| Agent tool UI | Configure inherit, selected, selected-empty, stale, and none policies | PENDING |
| Save & Apply | Change a managed HTTP/built-in tool; verify generation/hash increments without restart | PENDING |
| Active-call isolation | Keep a call active across reload; verify it retains old schema/config and next call gets new | PENDING |
| Failed reload | Submit invalid tool configuration; verify previous generation remains active | PENDING |
| Restart boundary | Change MCP/env/provider setting; verify UI reports restart required | PENDING |
| Browser console/API | No new console errors; Agent CRUD and reload endpoints return expected status | PENDING |

## Live-call regression gates on voiprnd

Run at least one selected-scope transfer and one denied-scope attempt on both AudioSocket
and ExternalMedia where configured. Confirm provider schema, spoken guidance, execution
result, Call History/audit generation, cleanup, and post-call hooks all agree. Then run the
configured provider/pipeline golden baseline set to exclude greeting, barge-in, farewell,
and transfer regressions. Archive call evidence before marking these rows PASS.
